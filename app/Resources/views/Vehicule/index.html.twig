{% extends 'base.html.twig' %}

{% block body %}
<div class="dropdown-header bg-danger">
<ul class="nav justify-content-end">
  <li class="nav-item">
    <a class="nav-link active" href="{{ path('ajout_voyageur') }}">Inscription</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="#">Reservation</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="#">Service</a>
  </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Depart</a>
            </li>
  <li class="nav-item">
    <a class="nav-link disabled" href="#">Contact</a>
  </li>
</ul></div>

        <div id="gauche">
    <div class="">
            <br/>
            <ul>
            <li><a href="{{ path('index_voyageur') }}">Liste voyageur</a></li>
            <li><a href="{{ path('vehicule_new') }}">Ajout vehicule</a></li>
            <li><a href="{{ path('index_vehicule') }}">Liste vehicule</a></li>
            </ul>

</div>
        </div>
<div id="droite">

        <div class="times">
<table class="table table-striped table-hover" id="action">

    <thead>
    <tr class="table-primary">
        <th>Numéro du vehicule</th>
        <th>Nom & Prenom</th>
        <th>Marque du vehicule</th>
        <th>Type du vehicule</th>
        <th>Nombre de place</th>
        <th>Disponibilité</th>
        <th>Action</th>
    </tr>
    </thead>
    <tbody>
 {% for vehicule in vehicules %}

    <tr>
        {#<td><a href="{{ path('index_voyageur', {'id':voyageur.id}) }}">{{ voyageur.id }}</a> </td># ~ " "~vehicule.voyageur.prenom#}
        <td>{{ vehicule.numero }}</td>
        <td>{{ vehicule.voyageur.nom~" "~ vehicule.voyageur.prenom}}</td>
        <td>{{ vehicule.marque}}</td>
        <td>{{ vehicule.type}}</td>
        <td>{{ vehicule.nombrePlace }}</td>
        <td>{{ vehicule.dispo}}</td>

{# {% if vehicule.disponibilite == 1 %}
                Oui
            {% elseif vehicule.disponibilite == 0 %}
            Non
            {% endif %} #}
        <td>
            <a href="#" class="btn-sm btn-light">
                <img src="{{ asset('images/png/search-alt.png') }}" alt="image" class="images">
            </a>
            <a href="#" class="btn-sm btn-light">
                <img src="{{ asset('images/png/edit.png') }}"  alt="image" class="images"></a>
            <a href= "{{ path("supprimer_vehicule", {'id': vehicule.id}) }}" class="btn-sm btn-light">
                <img src="{{ asset('images/png/close.png')  }}" alt="image" class="images">
            </a>
        </td>
    </tr>

{% endfor %}
    </tbody>

</table>
        </div>
    </div>
    </div>
{% endblock %}